spring:
  profiles:
    active: local

  application:
    name: boilerplate-notification

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  mail:
    host: smtp.gmail.com
    port: 587
    username: sensiplytesting@gmail.com
    password: etue dczt vwdw bvza
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true


    ssl:
      protocols: TLSv1.2
      trust: smtp.gmail.com

feign:
  retry:
    max: 1
    interval: 2000

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG
    org.springframework.cloud.sleuth: INFO
